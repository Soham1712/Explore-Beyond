create database explore;

use explore;

select * from results;

create table results(
 `Image_name` varchar (30),
  `results_alexnet` varchar (30),
  `results cnn` varchar (30),
  `prob_alexnet` varchar (30),
  `prob_cnn` varchar (30)
  );
  
